{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ],
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @portalpro/database db:generate && pnpm --filter @portalpro/email build"
}
