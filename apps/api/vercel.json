{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm build --filter=@portalpro/api",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
